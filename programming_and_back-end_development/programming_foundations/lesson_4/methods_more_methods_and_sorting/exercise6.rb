['ant', 'bear', 'caterpillar'].pop.size

# => 11
# pop returns last element from array (and mutates array)
# size is called on returned value ('caterpillar') and counts number
# of characters in string
